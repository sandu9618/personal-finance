@page
@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<h1>Dashboard</h1>
@if (!string.IsNullOrEmpty(Model.Error))
{
    <p class="text-danger">@Model.Error</p>
}

<section class="summary">
    <div>
        <span>Income</span>
        <strong>@Model.Dashboard.TotalIncome.ToString("0.00")</strong>
    </div>
    <div>
        <span>Expenses</span>
        <strong>@Model.Dashboard.TotalExpense.ToString("0.00")</strong>
    </div>
    <div>
        <span>Balance</span>
        <strong>@Model.Dashboard.Balanace.ToString("0.00")</strong>
    </div>
    <div>
        <span>Recent</span>
        <strong>@Model.Dashboard.RecentTransactions.Length</strong>
    </div>
</section>

<section class="panel">
<h2>Spending by category</h2>
<table class="table">
    <thead>
        <tr>
            <th>Category</th>
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Dashboard.ExpensesByCategory.Length == 0)
        {
            <tr>
                <td colspan="2">No expenses yet.</td>
            </tr>
        }
        @foreach (var row in Model.Dashboard.ExpensesByCategory)
        {
            <tr>
                <td>@row.CategoryName</td>
                <td>@row.Total.ToString("0.00")</td>
            </tr>
        }
    </tbody>
</table>
</section>

<section class="panel">
<h2>Recent transactions</h2>
<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Type</th>
            <th>Amount</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Dashboard.RecentTransactions.Length == 0)
        {
            <tr>
                <td colspan="4">No transactions yet.</td>
            </tr>
        }
        @foreach (var transaction in Model.Dashboard.RecentTransactions)
        {
            <tr>
                <td>@transaction.TransactionDate.ToString("yyyy-MM-dd")</td>
                <td>@transaction.Type</td>
                <td>@transaction.Amount.ToString("0.00")</td>
                <td>@transaction.Description</td>
            </tr>
        }
    </tbody>
</table>
</section>
